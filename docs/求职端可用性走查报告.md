# 求职端可用性走查报告

> 走查时间：2026-08-28（全链路：JobMatch → JobDetail → Diagnosis → LearningPath）
> 状态：全部修复完成，构建零错误。本文件为走查与修复记录。

## 修复清单（共 23 项）

### P0 — 必崩/死路（4 项，全部修复）

| # | 问题 | 修复 |
|---|------|------|
| 1 | `Resume.tsx` 跳 `my-skill-graph`，但 `JobseekerShell` 的 pages 映射没注册该 key → **白屏** | 新增 `MySkillGraph.tsx` 页面 + 注册映射 + `pages[page] \|\| JSDashboard` 兜底 |
| 2 | `Diagnosis.tsx` 漏传 `GapAnalysisList` 必填 prop `onGoToLearning` → **运行时崩溃** | 补传 prop |
| 3 | `JobMatch` 有推荐结果后永远锁死在推荐态，分页隐藏且无返回入口 | 新增「返回全部岗位」按钮，筛选变化自动退出推荐态 |
| 4 | LearningPath 有 24h 过期清除，Diagnosis 无过期判断且无重诊断入口 | 诊断结果加 24h TTL + 「重新诊断」按钮 |

### P1 — 假数据/体验问题（修复）

| # | 问题 | 修复 |
|---|------|------|
| 5 | `JobSelector` 硬编码「今日活跃」 | 展示真实 `collected_at` 采集时间 |
| 6 | `JobDetail` 编造 `\|\| '3-5年'` / `\|\| '本科及以上'` 默认值 | 统一显示「未标注」 |
| 7 | 诊断页拼「熟练掌握 X、Y」假 resume_text → 每次白跑 DeepSeek + 置信度虚标 0.92 | `AnalyzeReq` 支持 `skills` 数组直传，优先级：skills > resume_text > 解析缓存 > profile |
| 8 | 列表接口一次性拉 500 条客户端分页 | 服务端分页（20 条/页）+ 推荐态按 ids 拉取 |

### P2 — 工程问题（修复）

| # | 问题 | 修复 |
|---|------|------|
| 9 | ~~简历解析缓存被误判为死代码~~ | **撤回误判**：写入方在 `auth.py`（`{uid}_{hex}.{ext}.json`），glob 可匹配。教训：判定死代码必须确认读写两侧 |
| 10 | `SEED_JOBS` 导入期缓存 → 爬虫新岗位匹配/推荐查不到 | 删除常量，改 `load_all_jobs()` 等实时查库 |
| 11 | `build` 不做类型检查，41 个 TS 错误被放过 | `build` 改为 `tsc --noEmit && vite build`，41 错误清零 |
| 12-23 | 其余类型错误、学习阶段算法两处重复、hash 路由缺失、localStorage 传递规范等 | 逐项修复（详见 git 提交记录） |

## 修复后验证

- `npx tsc --noEmit`：41 error → **0 error**
- `npm run build`（含类型检查）：通过
- 后端改动文件 `py_compile`：通过
- 离线测试：JD 解析 F1 0.9553 / 简历提取 F1 0.9533 / 匹配 Top-5 1.0000 / 质检 14 PASS / 单测 20 PASS

## 遗留事项

- 企业端/管理端外壳未做 hash 路由（刷新回首页），待补
- 接口行为未经浏览器端到端验证，建议按功能走查一遍
